declare module '@shopify/ui-extensions/admin.app.home.render' {
  // Local fallback declaration for missing published types.
  // Keep as `any` to satisfy the CLI TypeScript checks until correct package versions are used.
  export type Api = any;
}
